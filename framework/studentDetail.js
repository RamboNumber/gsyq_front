
$(function () {
        init();
    }
)

function init() {
    queryDetail()
}

function queryDetail(id) {
    layui.use(['form','layer', 'table'], function () {
        var layer = layui.layer;
        layer.render({

        })
    })
}